backend:
  name: git-gateway
collections:
  - name: 'blog'
    label: 'Blog'
    folder: 'posts'
    fields:
      - {label: 'Title', name: 'title', widget: 'string'}
      - {label: 'Date', name: 'date', widget: 'datatime'}
      - {label: 'Author', name: 'author', widget: 'string'}
      - {label: 'Body', name: 'body', widget: 'markdown'}
media_folder: "samples"
media_library:
  name: cloudinary
  config:
    cloud_name: mmtar
    api_key: 268286452156171
    default_transformations:
      - fetch_format: auto
        quality: auto
        width: 400
        crop: scale


